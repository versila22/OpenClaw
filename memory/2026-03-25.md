## 2026-03-25 - Durable Memories (Pre-compaction Flush)

### Hotline Darons Project (Family IT Helpdesk L1)
- **Objective:** Telegram bot for family IT support, multimodal (audio/image via Gemini 2.5 Flash), zero-retention (RAM only), PII masking, L2 escalation to Jay.
- **Key Tech:** `python-telegram-bot`, `google-genai`, `dotenv`.
- **Deployment:** Docker (`Dockerfile`, `docker-compose.yml`) for production. `run.sh` script for native execution in OpenClaw container (handles venv, background).
- **Repo:** `https://github.com/versila22/hotline-darons`. `.gitignore` set.
- **Licensing:** PolyForm Noncommercial License 1.0.0 (Source-Available).
- **GitHub Bounties:** WhatsApp support, Local RAG, Streamlit Dashboard issues created.
- **Debugging Summary:**
    - Resolved `ModuleNotFoundError` for `flask` and `flask_bcrypt`.
    - Handled `telegram.error.Conflict` (multiple bot instances).
    - Fixed issue with images sent as `Document` instead of `Photo`.
    - Persistent `Permission denied` on VENV directories, resolved by `sudo rm -rf` and `chown`.
    - User's Telegram ID: `6390396611`.

### Content Factory Project (YouTube & Blog Automation)
- **Objective:** Generate weekly YouTube video scripts and blog articles from a Markdown summary.
- **Input:** Markdown file with condensed summary.
- **Outputs:** Markdown for YouTube script (3-5 min, 500-700 words), blog article (800-1200 words).
- **Persona:** Jay's "Architecte Pragmatique" style.
- **Tools:** Gemini for generation, ElevenLabs for high-quality voice cloning (Creator plan recommended for Professional Voice Cloning).
- **API Keys:** `GEMINI_API_KEY`, `ELEVENLABS_API_KEY` (configured in root workspace `.env`).

### Spotify Playlists Project
- **Objective:** Create 3 Spotify playlists (Deep Work, Baroque, Nature Sounds).
- **Debugging Summary:**
    - `spotipy` module not found, resolved by VENV installation.
    - `403 Insufficient client scope` due to Spotify Developer App permissions in "Development Mode".
    - `redirect_uri` changed to `https://example.com/callback`.
    - **Status:** Postponed due to complex Spotify Developer verification process for required scopes. The current application lacks necessary permissions.

### openclaw-youtube-uploader Project
- **Repo:** `https://github.com/versila22/openclaw-youtube-uploader`.
- **`README.md`:** Generated and committed.
- **Cron Job Issue:** Suspected failure to upload 3 videos to YouTube at 18:00.
- **Debugging Summary:**
    - No clear log file for the uploader found.
    - `youtube_env` VENV recreation attempt failed (`Permission denied` and `requirements.txt` missing).
    - Status: Requires `requirements.txt` confirmation and further VENV permission debugging.

### General Technical Learnings
- **API Key Security:** Critical incident of `GEMINI_API_KEY` leak on GitHub, requiring immediate revocation and Git history cleaning (`git filter-repo`). Emphasized `dotenv` and `.gitignore`.
- **OpenClaw UI:** `Control UI assets not found` error, resolved by `pnpm ui:build` on host.
- **Python Execution:** Frequent `python: command not found` resolved by using `python3` or VENV path.
- **Git Permissions:** `fatal: detected dubious ownership` and `insufficient permission for adding an object` in Git, resolved by `git config --global --add safe.directory` and `sudo chown -R`.
- **Spotify API:** Strict OAuth2 flow and scope limitations for applications in "Development Mode."

### Jay's Identity & Preferences
- **Surnom:** "L'Architecte" (based on `USER.md` and `SOUL.md`).
- **Communication:** Pragmatic, analytical, expert-to-expert. Prefers structured framing/constraints/technical solution.
- **Priorities:** Tokenomics, Security, Architecture.
- **Context:** AI Engineer & Project Manager.
