# 2026-04-09

- ai-finops heartbeat/cron: nouvelle erreur `401 Not authenticated` sur le endpoint de sync à 07:00 Europe/Paris.
- Vérification repo: les routes `/api/v1/sync` et `/api/v1/sync/all` sont protégées par `HTTPBearer` côté backend (`backend/app/auth.py`, `backend/app/api/v1/routes/providers.py`).
- Le frontend ajoute bien `Authorization: Bearer ...` quand un token existe (`src/lib/api.ts`), donc un cron qui appelle directement l’API sans JWT valide tombera logiquement en 401.
- Fix le plus probable: faire un login juste avant l’appel sync pour récupérer un JWT frais, ou créer un mécanisme dédié d’auth service-to-service pour l’automatisation.

- `gitleaks` installé via Linuxbrew dans le conteneur OpenClaw : `/data/linuxbrew/.linuxbrew/bin/gitleaks` (version `8.30.1`). Installation globale/outillage, pas dépendance d’un repo.
- `gog` vérifié installé via Linuxbrew dans le conteneur OpenClaw : `/data/linuxbrew/.linuxbrew/bin/gog` (version `v0.12.0`). Config/auth locale sous `~/.config/gogcli`; ce n’est pas une dépendance d’un repo.
- Références locales utiles pour `gog` : `labs/gog_setup_log.md` documente l’installation/config Google Workspace, et `memory/2026-04-07.md` note que l’auth Drive était OK.
