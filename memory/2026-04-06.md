# 2026-04-06

## LIMA App — Session nocturne (5-6 avril)

### Features livrées
- **Agenda admin : Modifier un événement** — `EditEventDialog` pré-rempli, PUT /events/{id}
- **Agenda admin : Supprimer un événement** — AlertDialog confirmation, DELETE /events/{id} avec suppression cascade manuelle des AlignmentAssignment/AlignmentEvent
- Boutons visibles uniquement pour les admins

### Infra Railway corrigée
- GitHub App Railway réinstallée (était déconnectée → "repo not found")
- `.env.production` ajouté avec `VITE_API_URL=https://api-production-e15b.up.railway.app`
- Endpoint `/admin/seed` (protégé par SEED_SECRET) + `/admin/reset-admin` ajoutés pour maintenance
- Seed complet tourné avec succès (membres, événements, lieux, saison, alignements)
- Login admin confirmé : `admin@lima-impro.fr` / `Admin1234!`
- Bug seed timezone corrigé (naive datetime pour TIMESTAMP WITHOUT TIME ZONE)

### Config OpenClaw
- Modèle principal : Claude Opus 4.6 (orchestration)
- Subagents code : ChatGPT 5.4
- Tâches légères : Gemini 2.5 Flash
- Gemma 4 abandonné (nécessite install locale, VPS insuffisant)

## AI FinOps — Session de build (soirée du 5 avril)

### Ce qui a été livré
- **Backend FastAPI complet** : 29 fichiers, SQLite, Pydantic v2, seed automatique
- **Frontend React branché** sur le backend via React Query + proxy Vite
- **Dockerisé** : `docker-compose.prod.yml` dans `/opt/openclaw/workspace/ai-finops/`
- **Exposé en HTTPS** via Traefik + Let's Encrypt sur `https://ai-finops-api.duckdns.org`
- **Frontend live** sur `https://ai-finops.lovable.app/` (Lovable)
- **Connecteurs réels** ajoutés : OpenAI (usage API) + ElevenLabs (subscription API)
- **Providers manuels** : Anthropic, Google, Lovable (éditables via PUT)
- Endpoints sync : `POST /api/v1/sync` et `POST /api/v1/providers/{id}/sync`

### Bug en cours (minuit)
- Après rebuild Docker avec connecteurs réels, le dashboard est **vide** (pas de données)
- Probablement un problème de seed ou de DB path dans le conteneur Docker
- En attente des logs Docker de Jerome (`docker compose logs`)
- La DB `finops.db` a été supprimée pour forcer le re-seed → possiblement le seed ne s'exécute pas

### DuckDNS
- Domaine : `ai-finops-api.duckdns.org` → `72.61.196.210`
- Compte DuckDNS connecté via GitHub (versila22)

### Vrais forfaits de Jerome
| Provider | Plan | Coût/mois | Quota |
|---|---|---|---|
| OpenAI | API pay-as-you-go | Variable | Usage réel en $ |
| Anthropic | Pro + API credits | ~$70 ($20+$50) | Tokens API |
| Google | One AI Premium 5To | 21,99€ | 1000 crédits IA |
| ElevenLabs | Creator | $22 | 100K characters |
| Lovable | Pro | ~$20 | 100 crédits |

### Infra notes
- Clés API backend dans `/opt/openclaw/workspace/ai-finops/backend/.env` (copié depuis `/opt/openclaw/.env`)
- `backend/.env` ajouté au `.gitignore` pour éviter les leaks
- Le conteneur ai-finops-backend utilise `env_file: ./backend/.env`
- Traefik config dans `/docker/traefik/docker-compose.yml` (network_mode: host, Let's Encrypt)

### Config OpenClaw (aussi fait ce soir — détails dans memory/2026-04-05.md)
- Modèles Anthropic ajoutés au menu déroulant
- GitHub auth via credential helper qui lit `/data/.openclaw/.gh/hosts.yml`
- Script credential helper dans `/data/.openclaw/workspace/.local/bin/git-credential-gh-hosts.sh`

### Préférence modèle
- Jerome préfère **Sonnet 4.6** pour les tâches de code structuré, **Opus** uniquement pour le raisonnement complexe
