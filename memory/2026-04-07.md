# 2026-04-07

## lima-app / déploiement — point d'architecture important
- Le backend réellement déployé par Railway vient du repo `lima-app` avec root directory `/backend`, pas du repo séparé `lima-backend`.
- Le repo `lima-backend` et `lima-app/backend` avaient divergé ; les endpoints `GET /members/me/planning` et `GET /api/admin/activity/*` existaient dans `lima-backend` mais pas dans `lima-app/backend`.
- Correction faite : synchronisation de `lima-backend` vers `lima-app/backend` puis push sur `versila22/lima-app` pour déclencher le redeploy Railway.
- Le frontend `limaimpro.duckdns.org` est servi depuis le VPS (72.61.196.210), pas automatiquement depuis GitHub ; un rebuild/redeploy frontend côté VPS peut être nécessaire après changement de `src/lib/api.ts`.

## Lima frontend build — rappel pratique
- Sur le VPS, `npm run build` dans `lima-app` a échoué avec `vite: not found`.
- Cause probable : dépendances frontend non installées sur le VPS dans ce dossier ; penser à lancer `npm ci` (ou `npm install`) avant le build.

## Veille boosts paris sportifs
- Suivi automatique des boosts Betclic/Unibet via X/Twitter non viable depuis le VPS : Cloudflare bloque l'IP datacenter pour Twitter, Nitter est derrière captcha/bot checks, Instagram est JS-only et Telegram n'a pas de chaîne officielle exploitable.

## Lima-app — session debug déploiement Railway (soirée)
- Frontend switché de lima-api.duckdns.org vers api-production-e15b.up.railway.app (VPS login cassé)
- Backend lima-app/backend/ synchronisé avec lima-backend (54 fichiers, +3553 lignes)
- Problèmes Railway successifs résolus : Alembic au démarrage, $PORT non interpolé dans railway.toml, Dockerfile multi-stage
- Backend Railway finalement up avec les nouveaux endpoints
- railway.toml startCommand = "uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1"
- Frontend rebuild sur VPS via npm ci --include=dev && npm run build + docker compose -f docker-compose.prod.yml up -d --build frontend

## Lima-app — bugs identifiés en soirée
- Joueurs Loisir (75€) classés en Cabaret ("C") car tranche 70-90€ = Cabaret dans import_service.py → fix à faire : ajouter colonne groupe dans CSV
- Commission "Programme" (comprog) absente → à créer manuellement via interface Admin
- Mon Planning vide pour Jerome admin → compte admin non lié aux alignment_assignments du membre "Jérôme Jacq"
- Stats connexions : table activity_logs probablement absente sur Railway (migrations non lancées)

## Drawer bottom sheet Agenda
- EventDetailDialog remplacé par EventDetailDrawer (vaul) sur Agenda.tsx → meilleur UX mobile
- Pushé sur lima-app main

## Lima-app — sessions de debug déploiement (soirée 07/04)

### Architecture clarifiée
- Backend Railway source = `lima-app/backend/` (repo `versila22/lima-app`, root dir `/backend`)
- Le repo séparé `lima-backend` (versila22/lima-backend) était **divergé** — endpoints `/me/planning` et `/api/admin/activity/*` existaient là mais pas dans `lima-app/backend/`
- Fix : rsync complet `lima-backend → lima-app/backend/` + push

### Problèmes Railway successifs résolus
1. `alembic upgrade head` en CMD → crash sur DB existante → retiré du Dockerfile
2. `railway.toml` override le Dockerfile CMD → `startCommand` avec `alembic` → même crash
3. `$PORT` non interpolé dans `railway.toml` → hardcodé à `8000`
4. Railway déployait un ancien commit au lieu du dernier → confusion entre redeploy/rebuild

### Bug import loisir/cabaret
- Les joueurs loisir (75€) tombent dans la tranche Cabaret (70-90€) dans `import_service.py`
- Fix à faire : ajouter colonne `groupe` dans fichier joueurs CSV ou ajuster les tranches de montants
- Codes player_status : C=Cabaret, M=Match, L=Loisir, A=Autre

### Mon Planning vide
- Pas un bug de code — l'admin n'a pas de `alignment_assignments` en DB pour son membre_id
- À corriger manuellement dans les données

### Frontend VPS
- `limaimpro.duckdns.org` → servi depuis VPS via Docker (`lima-frontend`)
- Rebuild : `npm ci --include=dev && npm run build` dans `/docker/openclaw-nmtd/data/.openclaw/workspace/lima-app`
- Redeploy : `docker compose -f docker-compose.prod.yml up -d --build frontend`
- L'URL API pointe maintenant sur Railway (`api-production-e15b.up.railway.app`)

### Alerte sécurité
- Scans de vulnérabilités détectés sur l'API : `/@fs/etc/passwd`, `/.git/config`, `/etc/passwd` — 40-68 requêtes chacun → à filtrer côté Traefik/middleware

## lima-app — corrections et architecture (soirée 07/04)

### Bug joueurs loisir classés en Cabaret
- Cause : règle `PLAYER_FEE_STATUS` dans `import_service.py` — tranche 70-90€ → "C" (Cabaret), mais loisir est aussi à ~75€
- Fix prévu : ajouter colonne `groupe`/`type_joueur` dans le CSV joueurs HelloAsso
- Code à modifier : `/lima/backend/app/services/import_service.py`

### Architecture déploiement lima-app finalisée
- Backend : Railway (auto-deploy depuis `lima-app/backend/` sur branche `main`)
- Frontend : VPS (`limaimpro.duckdns.org`), rebuild manuel via `npm ci --include=dev && npm run build` puis `docker compose -f docker-compose.prod.yml up -d --build frontend`
- API URL frontend : pointe maintenant sur `api-production-e15b.up.railway.app` (Railway), plus sur `lima-api.duckdns.org` (VPS cassé)
- railway.toml : `startCommand = "uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1"` (pas d'Alembic auto, pas de $PORT)
- Migrations Alembic nouvelles (activity_logs, app_settings, indexes) pas encore appliquées en prod Railway → à faire manuellement

### Endpoints manquants déployés
- `GET /members/me/planning` ✅ maintenant en prod Railway
- `GET /api/admin/activity/stats` ✅
- `GET /api/admin/activity/logins` ✅
- `GET /api/admin/activity/recent` ✅

### Mon Planning vide
- Pas un bug de code mais de données : le compte admin n'est pas lié aux alignment_assignments du membre "Jérôme Jacq"
- À corriger en base de données

### Alerte sécurité VPS
- Scans actifs sur l'API : `/@fs/etc/passwd` (68 req), `/.git/config` (40 req), `/etc/passwd` (38 req) → toutes 404/403 mais à surveiller

### Drawer bottom sheet (Agenda)
- EventDetailDialog remplacé par EventDetailDrawer (vaul) sur la page Agenda
- Commit `24d9a44` sur lima-app

## Real-Bayern Munich ce soir (quarts C1 aller)
- Diffusé sur Canal+ Foot/Sport à 21h00
- Jerome regarde Real-Bayern

## lima-app — Photos trombinoscope (à finir)
- PDF téléchargé depuis Drive : /tmp/trombinoscope.pdf (8.3 MB, 11 pages)
- Photos extraites dans /tmp/trombi_named/ mais mapping automatique peu fiable (blocs de noms groupés dans PDF)
- Jerome peut faire les identifications manuellement
- Plan : page HTML galerie pour identification → upload VPS → champ photo_url en DB → affichage dans Mon Profil + liste membres
- gog installé + auth Drive OK (GOG_KEYRING_PASSWORD=lima-gog-2026)
