# 2026-04-01

- Projet LIMA : les doublons d'événements agenda issus de l'import calendrier ont été nettoyés en base en supprimant uniquement les événements vides sans `alignment_assignments` ; les événements avec casting ont été conservés.
- Repo `versila22/lima-app` : README pro ajouté, secrets nettoyés, `backend/static/` retiré du git, docker-compose assaini pour utiliser des variables d'environnement ; push effectué sur `main`.
- Intégration future avec `lima.asso.fr` : approche recommandée = sous-domaine `app.lima.asso.fr` + simple lien "Espace Membres" dans la navbar du site prestataire.
- Railway : un nouveau service `api` a été créé dans le projet existant LIMA, avec domaine généré `api-production-e15b.up.railway.app` pour retenter un hébergement stable.
- OpenClaw / Gemini : incident de clé API Google/Gemini leakée ; nouvelle clé mise en place côté environnement Hostinger/OpenClaw et validée par test HTTP 200. Ne jamais stocker la clé en clair dans les fichiers mémoire/git.
