# 2026-04-08

- ai-finops heartbeat/cron: endpoint de sync a renvoyé `401 Not authenticated` à 07:00 Europe/Paris. Cause probable: appel `POST /sync` ou `/sync/all` sans header `Authorization: Bearer ...`. L’API protège ces routes avec `get_current_user` (JWT). Si cron conservait un JWT statique, risque aussi d’expiration (`JWT_EXPIRE_MINUTES=60`). Fix recommandé: authentifier juste avant l’appel sync ou créer un endpoint/service token dédié pour l’automatisation.
- LIMA auth heartbeat/cron: échec de login avec `jerome.jacq@gmail.com / test` à 10:00 Europe/Paris (`Email ou mot de passe incorrect`). Le secret erroné n’a pas été retrouvé dans le workspace par grep, donc il est probablement stocké dans la config du cron / secret store plutôt que dans le repo.
